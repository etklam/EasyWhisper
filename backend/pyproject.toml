[project]
name = "easywhisper-backend"
version = "0.1.0"
description = "Optional FastAPI backend for EASYWhisper"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
